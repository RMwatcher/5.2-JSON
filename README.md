This simple project simple take JSON data and turn it back into a JavaScript object to be used in this simple interactive website. The user simple clicks on an image and a name as well as a list of ingredients will display on the webpage.

This uses the following languages, libraries, and frameworks: node.js, express.js, EJS, and JSON. Note that the JSON data is inside the index.js file instead of a seperate file or api.
